[webservers]
%{ for i in webservers ~}
${i.name} ansible_host=${i.ansible_host} fqdn=${i.fqdn}
%{ endfor ~}

[databases]
%{ for i in databases ~}
${i.name} ansible_host=${i.ansible_host} fqdn=${i.fqdn}
%{ endfor ~}

[storage]
%{ for i in storage ~}
${i.name} ansible_host=${i.ansible_host} fqdn=${i.fqdn}
%{ endfor ~}
